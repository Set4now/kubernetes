apiVersion: v1
kind: Deployment
metadata:
	name: sample_voting_app
	labels:
     	app: sample_voting_app
spec:
	template:
		metadata: 
			labels: 
			   app: result_app
	    spec:
	    	containers:
	    	- name: result_app
	    	  image: dockersamples/examplevotingapp_result
	    	  ports:
	    	  - containerPort: 80
	replicas: 1
	selector:
		matchLabels:
			app: result_app